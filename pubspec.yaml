name: rayproject
description: A rice object detection using Flutter made by @protheeuz

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter



  cupertino_icons: ^0.1.3
  image_picker: ^0.6.7
  camera: ^0.5.8+7
  tflite: ^1.1.2
  tflite_flutter: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/models/yolo.tflite
    - assets/models/labels.txt

